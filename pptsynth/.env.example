# Authenticate the Claude Code CLI using one of its supported methods.
# Do not commit a populated .env file.
ANTHROPIC_API_KEY=

# Optional: set only when using a compatible external endpoint.
# ANTHROPIC_BASE_URL=

# Optional defaults for the router and synthesis stages.
# PPTSYNTH_MODEL=
# PPTSYNTH_EFFORT=low
